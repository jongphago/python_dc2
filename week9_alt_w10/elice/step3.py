from tkinter import Tk, Button, PhotoImage

tk = Tk()

# TODO: buttons, button_symbols 변수 선언
buttons = {}
button_symbols = {}

# TODO: symbols 변수 선언
symbols = []
for x in range(6):
    # PhotoImage 객체 생성
    img = None
    # symbols 리스트에 추가
    symbols
    symbols


for i in range(3):
    for j in range(4):
        btn = Button(width=100, height=100)
        btn.grid(row=i, column=j)

        # TODO: buttons, button_symbols에 버튼과 이미지 추가
        # buttons, button_symbols 딕셔너리에 버튼 추가
        buttons[i, j] = None
        # button_symbols 딕셔너리에 이미지 추가
        button_symbols[i, j] = None
        buttons[i, j]["image"] = None


tk.mainloop()
